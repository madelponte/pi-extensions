# Tool Profiles

Use `/profile readonly`, `/profile main`, or `/profile no-mcp`.

- `readonly`: enables `read`, `grep`, `find`, and `ls` only
- `main`: enables every registered tool
- `no-mcp`: enables everything except MCP bridge tools

Use `/profile-status` to inspect the current selection. Profiles last for the current Pi runtime/session.
